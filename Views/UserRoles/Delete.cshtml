@model Mash_v0._3.Models.sifProjectUserRole

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>sifProjectUserRole</legend>

    <div class="display-label">userRoleDescription</div>
    <div class="display-field">@Model.userRoleDescription</div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
      @Html.HiddenFor(model => model.idUserRole)
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}


